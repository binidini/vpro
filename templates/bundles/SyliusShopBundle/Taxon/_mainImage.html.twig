{% if taxon.images.first %}
    {% set path = taxon.images.first.path|imagine_filter(filter|default('sylius_shop_taxon_thumbnail')) %}
{% else %}
    {% set path = 'http://placehold.it/550x389' %}
{% endif %}

<img src="{{ path }}" alt="" class="ui bordered image" />
