{% import '@SyliusUi/Macro/messages.html.twig' as messages %}
{% import '@SyliusUi/Macro/pagination.html.twig' as pagination %}

{{ render(url('sylius_shop_partial_taxon_show_by_slug', {
    'slug': app.request.attributes.get('slug'),
    'template': '@SyliusShop/Taxon/_imageMenu.html.twig'
})) }}

<br/>
{{ render(path('bitbag_sylius_cms_plugin_shop_page_show', {'slug' : 'ttt'})) }}

{#{ bitbag_cms_render_block(app.request.attributes.get('slug')) }#}
